{
  "name": "nascar-pickem",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "functions",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "deploy": "NODE_OPTIONS=--max-old-space-size=8192 FUNCTIONS_DISCOVERY_TIMEOUT=120 firebase deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
